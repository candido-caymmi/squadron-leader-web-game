import Plane from "../../gameObjects/Plane";
// import MovementPhaseState from "./MovementPhaseState";
// import { TURN_HANDLER_EVENTS } from "../../constants/events";

export default class PlaneAttackState {
    constructor(plane) {
        this.plane = plane;
    }

    control(action) {
        
    }
}